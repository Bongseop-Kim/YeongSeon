# Toss 테스트 시크릿 키 — https://developers.tosspayments.com/sandbox
TOSS_SECRET_KEY=

# CORS 허용 출처
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# 사용하지 않는 키는 행 자체를 포함하지 않는다.
# 빈 문자열("")은 JavaScript/Deno에서 falsy이므로(MDN 참조),
# if (IMAGEKIT_PRIVATE_KEY) 등의 가드는 빈 값을 올바르게 걸러낸다.
